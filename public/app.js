const API='/api'; let token=localStorage.gp_token||''; let me=null; const $=s=>document.querySelector(s), app=$('#app');
function toast(m,ok=1){let d=document.createElement('div');d.className='toast '+(ok?'ok':'err');d.textContent=m;$('#toast').appendChild(d);setTimeout(()=>d.remove(),4200)}
async function api(url,opt={}){opt.headers={'Content-Type':'application/json',...(opt.headers||{})}; if(token)opt.headers.Authorization='Bearer '+token; if(opt.body&&typeof opt.body!='string')opt.body=JSON.stringify(opt.body); const r=await fetch(API+url,opt); const j=await r.json().catch(()=>({})); if(!r.ok)throw Error(j.error||'Hata oluştu'); return j}
function openModal(h){$('#modalContent').innerHTML=h;$('#modal').classList.remove('hidden')} function closeModal(){$('#modal').classList.add('hidden')} window.closeModal=closeModal;
async function loadMe(){if(!token){me=null;renderNav();return}try{me=await api('/me');renderNav()}catch{token='';localStorage.removeItem('gp_token');me=null;renderNav()}}
function renderNav(){$('#authBtn').textContent=me?me.name:'Giriş / Kayıt';$('#authBtn').onclick=()=>me?go('panel'):authModal();document.querySelector('[data-open-task]').onclick=()=>me?taskModal():authModal('Görev açmak için giriş yapmalısın.');document.querySelectorAll('[data-go]').forEach(a=>a.onclick=()=>go(a.dataset.go));}
function go(p){if(p==='tasks')return tasksPage(); if(p==='rules')return rulesPage(); if(p==='panel')return me?panelPage():authModal('Panele girmek için giriş yapmalısın.'); home()}
function authModal(msg=''){openModal(`<h2>Giriş / Kayıt</h2>${msg?`<p class="badge">${msg}</p>`:''}<div class="two"><div class="card"><h3>Giriş yap</h3><form class="form" id="loginF"><input name="email" placeholder="E-posta"><input name="password" type="password" placeholder="Şifre"><button class="primary">Giriş Yap</button></form></div><div class="card"><h3>Kayıt ol</h3><form class="form" id="regF"><input name="name" placeholder="Ad Soyad"><input name="email" placeholder="E-posta"><input name="password" type="password" placeholder="Şifre, min 6"><button class="primary">Kayıt Ol</button></form></div></div>`);$('#loginF').onsubmit=async e=>{e.preventDefault();try{let j=await api('/login',{method:'POST',body:Object.fromEntries(new FormData(e.target))});token=j.token;localStorage.gp_token=token;await loadMe();closeModal();toast('Giriş başarılı');home()}catch(er){toast(er.message,0)}};$('#regF').onsubmit=async e=>{e.preventDefault();try{let j=await api('/register',{method:'POST',body:Object.fromEntries(new FormData(e.target))});token=j.token;localStorage.gp_token=token;await loadMe();closeModal();toast('Kayıt başarılı')}catch(er){toast(er.message,0)}}}
function home(){app.innerHTML=`<section class="hero"><div><span class="pill">Emanet ödeme • Güvenli teslim • Komisyonlu sistem</span><h1>Küçük işleri yaptır, görev yaparak kazanç sağla.</h1><p>Fotoğraf çekimi, araştırma, sosyal medya desteği, kurye benzeri küçük işler ve dijital görevler için profesyonel mikro görev pazarı.</p><div class="actions"><button class="primary" onclick="${me?'taskModal()':'authModal(\'Görev açmak için giriş yapmalısın.\')'}">İlk görevini aç</button><button class="ghost" onclick="tasksPage()">Görevlere bak</button></div><div class="stats"><span><b>₺50+</b><br>minimum görev</span><span><b>%15</b><br>platform komisyonu</span><span><b>7/24</b><br>başvuru akışı</span></div></div><div class="showcase"><div class="row"><b>🟢 Canlı görev örneği</b><span class="funded">Finanse edildi</span></div><h2>Elazığ merkezde mağaza vitrini fotoğrafı çekilecek</h2><p>Konuma gidip 5 net fotoğraf çekilecek. Teslim sonrası ödeme serbest bırakılır.</p><div class="price">₺250 <small>• teslim 2 saat</small></div><br><div class="bar" onclick="tasksPage()">Başvur</div></div></section><section class="steps"><div class="card"><span class="badge">1</span><h3>Görev aç</h3><p>Bütçe, şehir, süre ve açıklamayı yaz.</p></div><div class="card"><span class="badge">2</span><h3>Ödeme emanet</h3><p>Para görev bitene kadar platformda bekler.</p></div><div class="card"><span class="badge">3</span><h3>Teslim ve onay</h3><p>İş onaylanınca ödeme görev yapana geçer.</p></div></section><section class="section"><h2>Görev kategorileri</h2><div class="categories">${['Fotoğraf & Video','Sosyal Medya','Araştırma','Yazı & Çeviri','Kurye & Teslimat','Diğer'].map(x=>`<div class="cat">${x}</div>`).join('')}</div></section><section id="homeTasks" class="section"><h2>Öne çıkan görevler</h2><div id="taskList" class="taskgrid"></div></section><div class="footer"><span>Hemen kayıt ol, görev aç veya görev yaparak kazanmaya başla!</span><button onclick="authModal()">Kayıt Ol</button></div>`;loadTasks('', '', '', '#taskList', 3)}
async function loadTasks(q='',category='',city='',target='#tasks',limit=0){try{let rows=await api(`/tasks?q=${encodeURIComponent(q)}&category=${encodeURIComponent(category)}&city=${encodeURIComponent(city)}`); if(limit)rows=rows.slice(0,limit); $(target).innerHTML=rows.map(t=>taskCard(t)).join('')||'<div class="card">Görev bulunamadı.</div>'}catch(e){toast(e.message,0)}}
function taskCard(t){return `<div class="card task"><span class="badge green">${t.payment_status==='funded'?'Finanse edildi':t.payment_status}</span><h3>${t.title}</h3><p>${t.description}</p><div class="meta"><span>📍 ${t.city}</span><span>⏱ ${t.duration}</span><span>👥 ${t.app_count||0} başvuru</span></div><div class="row"><div class="price">₺${t.budget}</div><button class="primary" onclick="taskDetail('${t.id}')">Detay / Başvur</button></div></div>`}
function tasksPage(){app.innerHTML=`<section class="section"><div class="row"><h1>Açık görevler</h1><button class="primary" onclick="${me?'taskModal()':'authModal(\'Görev açmak için giriş yapmalısın.\')'}">+ Görev Aç</button></div><div class="toolbar"><input id="q" placeholder="Görev ara..."><select id="cat"><option value="">Tüm kategoriler</option>${['Fotoğraf & Video','Sosyal Medya','Araştırma','Yazı & Çeviri','Kurye & Teslimat','Diğer'].map(x=>`<option>${x}</option>`).join('')}</select><input id="city" placeholder="Şehir"><button id="fbtn">Yenile</button></div><div id="tasks" class="taskgrid"></div></section>`; let f=()=>loadTasks($('#q').value,$('#cat').value,$('#city').value); $('#fbtn').onclick=f;['q','cat','city'].forEach(id=>$(`#${id}`).oninput=f); f()}
function proofView(sub){let parts=[]; if(sub.proof)parts.push(`<p><b>Kanıt linki/açıklama:</b> ${esc(sub.proof)}</p>`); if(sub.proof_data)parts.push(`<button onclick="openProof('${sub.id}')">Yüklenen dosyayı aç: ${esc(sub.proof_name||'kanıt dosyası')}</button>`); if(sub.revision_note)parts.push(`<p class="badge">Revizyon notu: ${esc(sub.revision_note)}</p>`); if(sub.dispute_reason)parts.push(`<p class="badge">Uyuşmazlık sebebi: ${esc(sub.dispute_reason)}</p>`); if(sub.admin_note)parts.push(`<p class="badge">Admin notu: ${esc(sub.admin_note)}</p>`); return parts.join('')||'<p>Kanıt girilmemiş.</p>'}
function submissionCard(sub,t,mine){let actions=''; if(mine&&sub.status==='submitted') actions=`<div class="row"><button class="primary" onclick="approveTask('${t.id}')">Onayla ve Ödemeyi Serbest Bırak</button><button onclick="requestRevision('${sub.id}')">Revizyon İste</button><button onclick="disputeSubmission('${sub.id}')">Uyuşmazlık Aç</button></div>`; return `<div class="card"><div class="row"><b>${esc(sub.user_name)}</b><span class="badge">${submissionStatus(sub.status)}</span></div><p>${esc(sub.note||'')}</p>${proofView(sub)}${actions}</div>`}
function submissionStatus(s){return {submitted:'Teslim edildi',revision_requested:'Revizyon istendi',disputed:'Uyuşmazlıkta',approved:'Onaylandı',rejected:'Reddedildi'}[s]||s}
async function fileToBase64(file){return new Promise((res,rej)=>{let r=new FileReader(); r.onload=()=>res(String(r.result)); r.onerror=rej; r.readAsDataURL(file)})}
async function openProof(id){try{let t=await currentTaskForProof; let sub=(t.submissions||[]).find(x=>x.id===id); if(!sub||!sub.proof_data)return toast('Dosya bulunamadı',0); let w=window.open(''); w.document.write(`<title>${esc(sub.proof_name||'kanıt')}</title><iframe src="${sub.proof_data}" style="border:0;width:100%;height:100vh"></iframe>`)}catch(e){toast('Dosya açılamadı',0)}}
let currentTaskForProof=null;
async function taskDetail(id){try{let t=await api('/tasks/'+id); currentTaskForProof=t; let mine=me&&t.owner_id===me.id; let assigned=me&&t.assigned_to===me.id; let submissions=(t.submissions||[]).map(s=>submissionCard(s,t,mine)).join('')||'<p>Henüz teslim yok.</p>'; openModal(`<h2>${esc(t.title)}</h2><p>${esc(t.description)}</p><p class="meta">📍 ${esc(t.city)} • ${esc(t.category)} • ${esc(t.duration)}</p><div class="price">₺${t.budget}</div><p><span class="badge">Durum: ${esc(t.status)}</span></p><hr><h3>Başvurular</h3><div>${(t.applications||[]).map(a=>`<div class="card"><b>${esc(a.user_name)}</b><p>${esc(a.message)}</p><span class="badge">${esc(a.status)}</span>${mine&&a.status==='pending'?` <button onclick="acceptApp('${a.id}')">Kabul Et</button>`:''}</div>`).join('')||'Henüz başvuru yok.'}</div><hr><h3>Teslim Kanıtları</h3>${submissions}<hr>${!me?'<button class="primary" onclick="authModal(\'Başvurmak için giriş yapmalısın.\')">Giriş yap ve başvur</button>':mine?'Bu senin görevin. Teslim gelince kanıtları burada görürsün.': t.status==='open'?`<form id="applyF" class="form"><textarea name="message" placeholder="Başvuru mesajın"></textarea><button class="primary">Başvur</button></form>`: assigned&&t.status==='in_progress'?`<form id="subF" class="form"><textarea name="note" placeholder="Teslim notu: görevi nasıl yaptığını anlat"></textarea><input name="proof" placeholder="Kanıt linki veya kısa açıklama"><input name="proof_file" type="file" accept="image/*,.pdf,.zip,.txt"><small>Dosya: ekran görüntüsü, PDF, ZIP veya metin. En fazla yaklaşık 4 MB önerilir.</small><button class="primary">Teslim Et</button></form>`: assigned&&t.status==='submitted'?'<p class="badge">Teslim gönderildi, görev sahibinin onayı bekleniyor.</p>': assigned&&t.status==='disputed'?'<p class="badge">Uyuşmazlık admin kararında.</p>':''}`); let af=$('#applyF'); if(af)af.onsubmit=async e=>{e.preventDefault();try{await api('/tasks/'+id+'/apply',{method:'POST',body:Object.fromEntries(new FormData(e.target))});toast('Başvuru gönderildi');closeModal();tasksPage()}catch(er){toast(er.message,0)}}; let sf=$('#subF'); if(sf)sf.onsubmit=async e=>{e.preventDefault();try{let fd=new FormData(e.target); let body={note:fd.get('note'),proof:fd.get('proof')}; let f=fd.get('proof_file'); if(f&&f.size){if(f.size>4*1024*1024)throw Error('Dosya çok büyük. En fazla yaklaşık 4 MB yükleyin.'); body.proof_name=f.name; body.proof_type=f.type; body.proof_data=await fileToBase64(f)} await api('/tasks/'+id+'/submit',{method:'POST',body});toast('Teslim kanıtı gönderildi');closeModal();panelPage('assigned')}catch(er){toast(er.message,0)}}}catch(e){toast(e.message,0)}}
async function acceptApp(id){try{await api('/applications/'+id+'/accept',{method:'POST'});toast('Başvuru kabul edildi');closeModal();panelPage()}catch(e){toast(e.message,0)}}
async function approveTask(id){try{let r=await api('/tasks/'+id+'/approve',{method:'POST'});toast(r.message);closeModal();panelPage()}catch(e){toast(e.message,0)}}
async function requestRevision(id){let note=prompt('Revizyon sebebini yaz. Örn: ekran görüntüsü eksik, açıklama yetersiz.'); if(!note)return; try{let r=await api('/submissions/'+id+'/revision',{method:'POST',body:{note}});toast(r.message);closeModal();panelPage('owned')}catch(e){toast(e.message,0)}}
async function disputeSubmission(id){let reason=prompt('Uyuşmazlık sebebini yaz. Admin bunu görecek.'); if(!reason)return; try{let r=await api('/submissions/'+id+'/dispute',{method:'POST',body:{reason}});toast(r.message);closeModal();panelPage('owned')}catch(e){toast(e.message,0)}}
function taskModal(){openModal(`<h2>Görev Aç</h2><form class="form" id="taskF"><input name="title" placeholder="Başlık"><div class="two"><select name="category"><option>Fotoğraf & Video</option><option>Sosyal Medya</option><option>Araştırma</option><option>Yazı & Çeviri</option><option>Kurye & Teslimat</option><option>Diğer</option></select><input name="city" placeholder="Şehir / Online"></div><div class="two"><input name="budget" type="number" min="50" placeholder="Bütçe TL"><input name="duration" placeholder="Süre"></div><textarea name="description" placeholder="Görev açıklaması"></textarea><button class="primary">Kaydet ve Ödemeye Geç</button></form>`);$('#taskF').onsubmit=async e=>{e.preventDefault();try{let r=await api('/tasks',{method:'POST',body:Object.fromEntries(new FormData(e.target))});closeModal();paymentModal(r.id)}catch(er){toast(er.message,0)}}}
function paymentModal(id){openModal(`<h2>Ödeme Emanet</h2><p>PayTR bilgileri eklenene kadar demo ödeme ile görev akışı test edilir. PayTR hesabın onaylanınca aynı buton canlı ödeme başlatır.</p><button class="primary" id="payBtn">PayTR ile Öde / Demo Finanse Et</button>`);$('#payBtn').onclick=async()=>{try{let r=await api('/payments/paytr/create',{method:'POST',body:{task_id:id}});toast(r.message);closeModal();tasksPage()}catch(e){toast(e.message,0)}}}

function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
async function profileModal(){if(!me)return authModal(); openModal(`<h2>Profil Bilgileri</h2><p class="muted">Telefon numarası sadece admin panelinde görünür. IBAN kayıt sırasında istenmez; para çekme sırasında girilir.</p><form id="profileF" class="form"><input name="name" placeholder="Ad Soyad" value="${esc(me.name)}"><input name="phone" placeholder="Telefon / WhatsApp" value="${esc(me.phone||'')}"><button class="primary">Profili Kaydet</button></form>`); $('#profileF').onsubmit=async e=>{e.preventDefault();try{let r=await api('/profile',{method:'PUT',body:Object.fromEntries(new FormData(e.target))});toast(r.message);await loadMe();closeModal();panelPage('withdrawals')}catch(er){toast(er.message,0)}}}
function withdrawModal(){if(!me)return authModal(); openModal(`<h2>Para Çekme Talebi</h2><p class="muted">Minimum çekim tutarı 50 TL. IBAN sadece sen ve admin tarafından görülür.</p><form id="withdrawF" class="form"><input name="amount" type="number" min="50" max="${me.balance||0}" placeholder="Çekilecek tutar TL"><input name="full_name" placeholder="IBAN sahibi ad soyad" value="${esc(me.name)}"><input name="iban" placeholder="TR ile başlayan IBAN"><button class="primary">Talep Oluştur</button></form>`); $('#withdrawF').onsubmit=async e=>{e.preventDefault();try{let r=await api('/withdrawals',{method:'POST',body:Object.fromEntries(new FormData(e.target))});toast(r.message);await loadMe();closeModal();panelPage('withdrawals')}catch(er){toast(er.message,0)}}}
function withdrawalBadge(s){return s==='paid'?'Ödendi':s==='rejected'?'Reddedildi':'Beklemede'}
async function panelPage(tab='owned'){if(!me)return authModal(); await loadMe(); let d=await api('/dashboard'); let withdrawals=await api('/withdrawals'); app.innerHTML=`<section class="section"><div class="row"><h1>Kullanıcı paneli</h1><button onclick="logout()">Çıkış</button></div><div class="panel"><div class="card side"><button onclick="panelPage('owned')">Açtığım görevler</button><button onclick="panelPage('assigned')">Aldığım görevler</button><button onclick="panelPage('apps')">Başvurularım</button><button onclick="panelPage('withdrawals')">Bakiye / Para Çek</button><button onclick="profileModal()">Profil Bilgileri</button>${me.is_admin?'<button onclick="adminPage()">Admin Paneli</button>':''}<div class="card"><b>Bakiye</b><div class="price">₺${me.balance||0}</div><button class="primary" onclick="withdrawModal()">Para Çek</button></div></div><div id="panelContent"></div></div></section>`; if(tab==='withdrawals'){ $('#panelContent').innerHTML=`<div class="card"><h2>Bakiye ve para çekme</h2><p>Mevcut bakiye: <b>₺${me.balance||0}</b></p><button class="primary" onclick="withdrawModal()">Para Çekme Talebi Oluştur</button><button onclick="profileModal()">Profil Bilgilerini Düzenle</button></div><h2>Çekim taleplerim</h2><table class="table"><tr><th>Tutar</th><th>Ad Soyad</th><th>IBAN</th><th>Durum</th><th>Tarih</th></tr>${withdrawals.map(w=>`<tr><td>₺${w.amount}</td><td>${esc(w.full_name)}</td><td>${esc(w.iban)}</td><td><span class="badge">${withdrawalBadge(w.status)}</span></td><td>${new Date(w.created_at).toLocaleString('tr-TR')}</td></tr>`).join('')||'<tr><td colspan="5">Henüz para çekme talebin yok.</td></tr>'}</table>`; return } let rows=d[tab]||[]; $('#panelContent').innerHTML=`<div class="taskgrid">${rows.map(t=>`<div class="card"><span class="badge">${t.status}</span><h3>${esc(t.title)}</h3><p>₺${t.budget||''}</p><button onclick="taskDetail('${t.task_id||t.id}')">Detay</button></div>`).join('')||'<div class="card">Kayıt yok.</div>'}</div>`}
async function setWithdrawalStatus(id,status){try{let note=status==='rejected'?prompt('Red sebebi / not yazabilirsiniz:')||'':''; let r=await api('/admin/withdrawals/'+id+'/status',{method:'POST',body:{status,admin_note:note}});toast(r.message);adminPage()}catch(e){toast(e.message,0)}}
async function resolveDispute(id,decision){try{let note=prompt(decision==='worker'?'Görev yapan lehine karar notu:':'Revizyon kararı notu:')||''; let r=await api('/admin/disputes/'+id+'/resolve',{method:'POST',body:{decision,admin_note:note}});toast(r.message);adminPage()}catch(e){toast(e.message,0)}}
async function adminPage(){try{let s=await api('/admin/stats'); let users=await api('/admin/users'); let tasks=await api('/admin/tasks'); let withdrawals=await api('/admin/withdrawals'); let disputes=await api('/admin/disputes'); app.innerHTML=`<section class="section"><h1>Admin Paneli</h1><div class="grid"><div class="card"><b>Kullanıcı</b><div class="price">${s.users}</div></div><div class="card"><b>Görev</b><div class="price">${s.tasks}</div></div><div class="card"><b>Ödeme Hacmi</b><div class="price">₺${s.volume}</div></div><div class="card"><b>Çekim Talebi</b><div class="price">${s.withdrawals||0}</div></div></div><h2>Uyuşmazlık Merkezi</h2><table class="table"><tr><th>Görev</th><th>Görev Sahibi</th><th>Görev Yapan</th><th>Kanıt</th><th>Sebep</th><th>İşlem</th></tr>${disputes.map(d=>`<tr><td>${esc(d.task_title)}<br><small>₺${d.budget}</small></td><td>${esc(d.owner_name)}<br><small>${esc(d.owner_email)}</small></td><td>${esc(d.worker_name)}<br><small>${esc(d.worker_email)}</small></td><td>${esc(d.note||'')}<br>${d.proof?`<small>${esc(d.proof)}</small>`:''}${d.proof_data?`<br><button onclick="adminOpenProof('${d.id}')">Dosyayı Aç</button>`:''}</td><td>${esc(d.dispute_reason||'-')}</td><td><button onclick="resolveDispute('${d.id}','worker')">Görev Yapan Haklı</button><button onclick="resolveDispute('${d.id}','revision')">Revizyona Gönder</button></td></tr>`).join('')||'<tr><td colspan="6">Uyuşmazlık yok.</td></tr>'}</table><h2>Para Çekme Talepleri</h2><table class="table"><tr><th>Kullanıcı</th><th>Telefon</th><th>Tutar</th><th>IBAN</th><th>Durum</th><th>İşlem</th></tr>${withdrawals.map(w=>`<tr><td>${esc(w.user_name)}<br><small>${esc(w.user_email)}</small></td><td>${esc(w.user_phone||'-')}</td><td>₺${w.amount}</td><td>${esc(w.iban)}<br><small>${esc(w.full_name)}</small></td><td><span class="badge">${withdrawalBadge(w.status)}</span></td><td>${w.status==='pending'?`<button onclick="setWithdrawalStatus('${w.id}','paid')">Ödendi</button> <button onclick="setWithdrawalStatus('${w.id}','rejected')">Reddet</button>`:'-'}</td></tr>`).join('')||'<tr><td colspan="6">Talep yok.</td></tr>'}</table><h2>Kullanıcılar</h2><table class="table"><tr><th>Ad</th><th>E-posta</th><th>Telefon</th><th>Bakiye</th></tr>${users.map(u=>`<tr><td>${esc(u.name)}</td><td>${esc(u.email)}</td><td>${esc(u.phone||'-')}</td><td>₺${u.balance}</td></tr>`).join('')}</table><h2>Görevler</h2><table class="table"><tr><th>Başlık</th><th>Durum</th><th>Bütçe</th></tr>${tasks.map(t=>`<tr><td>${esc(t.title)}</td><td>${esc(t.status)}</td><td>₺${t.budget}</td></tr>`).join('')}</table></section>`; window._adminDisputes=disputes}catch(e){toast(e.message,0)}}
function adminOpenProof(id){let d=(window._adminDisputes||[]).find(x=>x.id===id); if(!d||!d.proof_data)return toast('Dosya yok',0); let w=window.open(''); w.document.write(`<title>${esc(d.proof_name||'kanıt')}</title><iframe src="${d.proof_data}" style="border:0;width:100%;height:100vh"></iframe>`)}
function logout(){token='';localStorage.removeItem('gp_token');me=null;renderNav();home();toast('Çıkış yapıldı')} window.profileModal=profileModal; window.withdrawModal=withdrawModal; window.setWithdrawalStatus=setWithdrawalStatus; window.logout=logout; window.authModal=authModal; window.taskModal=taskModal; window.tasksPage=tasksPage; window.taskDetail=taskDetail; window.acceptApp=acceptApp; window.approveTask=approveTask; window.panelPage=panelPage; window.adminPage=adminPage; window.requestRevision=requestRevision; window.disputeSubmission=disputeSubmission; window.openProof=openProof; window.resolveDispute=resolveDispute; window.adminOpenProof=adminOpenProof;
function rulesPage(){app.innerHTML=`<section class="section"><h1>Kurallar</h1><div class="grid"><div class="card"><h3>Yasaklı görevler</h3><p>Şiddet, taciz, dolandırıcılık, yasa dışı işler, hack, spam ve insanlık dışı görevler yasaktır.</p></div><div class="card"><h3>Emanet ödeme</h3><p>Görev bedeli görev tamamlanana kadar platformda bekler.</p></div><div class="card"><h3>Komisyon</h3><p>Onaylanan işlerde %15 platform komisyonu uygulanır.</p></div></div></section>`}
loadMe().then(home);
